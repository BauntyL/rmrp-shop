{
  "name": "rest-express",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "npm install --include=dev && vite build && node copy-static.js"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^4.0.0"
  }
}