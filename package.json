{
  "name": "rest-express",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "npm install --include=dev && vite build && cp -r server dist/ && cp -r shared dist/ && cp storage.js dist/ && cp server/db.js dist/ && cp server/index.js dist/server/index.js && cp package.json dist/ && cp package-lock.json dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}